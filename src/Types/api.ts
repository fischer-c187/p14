export type Employee = {
  _id: string;
  firstName: string;
  lastName: string;
  dateBirth: string;
  startDate: string;
  street: string;
  city: string;
  state: string;
  zipCode: string;
  department: string;
};

export type ListEmployee = Employee[];
